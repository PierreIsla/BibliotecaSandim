@page
@model BibliotecaSandim.Pages.Membros.IndexModel

@{
    ViewData["Title"] = "Membros";
}

<div class="container mt-4">

    <div class="card shadow-sm">
        <div class="card-body">

            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <h2 class="mb-0">Membros</h2>
                    <small class="text-muted">
                        Gestão de membros registados na biblioteca
                    </small>
                </div>

                <a asp-page="Create" class="btn btn-success">
                    + Criar Membro
                </a>
            </div>

            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Nome</th>
                        <th>Email</th>
                        <th>Data de Inscrição</th>
                        <th class="text-center">Ações</th>
                    </tr>
                </thead>

                <tbody>
                @foreach (var item in Model.Membro)
                {
                    <tr>
                        <td>@item.Nome</td>

                        <td>
                            <small class="text-secondary">
                                @item.Email
                            </small>
                        </td>

                        <td>
                            @item.DataInscricao.ToString("dd/MM/yyyy")
                        </td>

                        <td class="text-center">
                            <a asp-page="./Edit"
                               asp-route-id="@item.MembroID"
                               class="btn btn-sm btn-warning">
                                Editar
                            </a>

                            <a asp-page="./Details"
                               asp-route-id="@item.MembroID"
                               class="btn btn-sm btn-info text-white">
                                Detalhes
                            </a>

                            <a asp-page="./Delete"
                               asp-route-id="@item.MembroID"
                               class="btn btn-sm btn-danger">
                                Eliminar
                            </a>
                        </td>
                    </tr>
                }
                </tbody>
            </table>

        </div>
    </div>

</div>
